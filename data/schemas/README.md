# Schemas

Status: planned schema structure, not runtime-wired.

Future machine-readable schemas and schema notes can live here. Adding a schema does not validate runtime data unless a route or provider PR explicitly wires it.
