# Registries

Status: planned registry structure, not runtime-wired.

Future checked-in registries that are safe to publish can live here. The active source registry remains in `src/sources/master-source-registry.js` until a separate migration PR.
