# Server Handlers

Status: planned handler structure, not wired.

Future shared route handlers belong here when Netlify Functions and Cloudflare-compatible runtimes need the same behavior. Handlers should own API envelopes, structured errors, and compatibility behavior without depending on a single hosting platform.
