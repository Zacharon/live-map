# Server Middleware

Status: planned middleware structure, not wired.

Future platform-neutral guardrails belong here: rate limits, bounded JSON parsing, request-size checks, CORS/header helpers, and sanitized error handling. Middleware must fail safely and must not expose stack traces or secrets.
