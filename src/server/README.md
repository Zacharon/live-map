# Server Structure

Status: planned shared server structure, not wired.

This folder is for platform-neutral server code that can be shared by Netlify Functions, Cloudflare Workers, and future Cloudflare Pages Functions. Runtime wrappers should stay thin and preserve existing route names and response shapes.
