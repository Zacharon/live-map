# Shared Utilities

Status: planned shared utility structure, not wired.

Put only runtime-neutral helpers here. Shared code must avoid browser globals, Node-only APIs, platform request objects, provider credentials, and feature-specific policy.
