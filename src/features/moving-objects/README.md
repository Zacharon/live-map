# Moving Objects Feature

Status: planned ownership folder, not wired.

Owns future bounded aviation and maritime object concepts. Moving-object work must stay viewport-limited, cached, capped, and privacy-safe; existing runtime code remains in `src/moving-objects/` and Netlify functions.
