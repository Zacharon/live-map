# Events Feature

Status: planned ownership folder, not wired.

Owns future event-feed domain logic, canonical event concepts, and event filter behavior. Existing runtime code remains in `src/events/`, `src/api/events-response.js`, and frontend controllers until a scoped migration PR.
