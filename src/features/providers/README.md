# Providers Feature

Status: planned ownership folder, not wired.

Owns future provider capability concepts shared across adapters. Individual provider integrations remain in `src/data/providers/`, and provider credentials must stay server-side.
