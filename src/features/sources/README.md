# Sources Feature

Status: planned ownership folder, not wired.

Owns future Source Explorer domain logic and source metadata view models. Existing source registry behavior remains in `src/sources/` and `src/source-explorer-app.js`.
